%h1 Athlete Performance: Per Session
%h2= @athlete.fullname
%h3= @athlete.email

- @s = pie_chart_athlete(@athlete.id)
= image_tag @s.to_url

%table
  %thead
    %th Training Day
    %th Template
    %th{:colspan => 2} Action
  %tbody
    - @athlete.tsessions.each do |s|
      %tr
        %td= s.trainingday
        %td= s.template.name
        - if s.is_done
          %td= link_to 'logs', "/tsessions/#{s.id}"
        - elsif
          %td logs not available
